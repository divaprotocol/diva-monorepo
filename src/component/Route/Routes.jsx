
/*import React from 'react'
import { BrowserRouter as Router, Switch, Route, Link } from "react-router-dom"
import Underlying from "../Trade/Underlying"
import { useLocation } from "react-router-dom"
import OptionsList from '../Markets/OptionsList'

export default function Routes() {
    //const location = useLocation()
    //console.log(location.state.optionData)
    const optionData = location.state.optionData
    return(
        <Router>
        <Switch>
            <Route exact path="/"><OptionsList optionData={optionData}/></Route>
            <Route path="/Trade"><Underlying optionData={location.state.optionData}/></Route>
        </Switch>
        </Router>
    )
}*/