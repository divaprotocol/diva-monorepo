{
  "name": "diva-tellor-subgraph",
  "license": "UNLICENSED",
  "version": "0.0.1",
  "scripts": {
    "lint": "yarn codegen && yarn build",
    "codegen": "graph codegen",
    "build": "yarn codegen && graph build",
    "graph:auth": "graph auth --studio $THEGRAPH_ACCESS_TOKEN",
    "deploy-goerli": "graph deploy --studio $THEGRAPH_ACCESS_SLUG",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.37.1",
    "@graphprotocol/graph-ts": "^0.29.0",
    "prettier": "^2.5.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
